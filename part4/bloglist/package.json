{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production dotenvx run -- node index.js",
    "dev": "cross-env NODE_ENV=development dotenvx run -- nodemon index.js",
    "lint": "eslint .",
    "test": "cross-env NODE_ENV=test dotenvx run -- jest --verbose --runInBand",
    "start:test": "dotenvx run -- cross-env NODE_ENV=test node index.js"
  },
  "author": "oatlayers",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.8.7"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.57.1",
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node",
    "globalTeardown": "./tests/teardown.js"
  }
}
